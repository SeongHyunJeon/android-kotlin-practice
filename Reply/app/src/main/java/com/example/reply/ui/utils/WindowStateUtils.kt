package com.example.reply.ui.utils

enum class ReplyNavigationType { //탐색 유형 종류.
    BOTTOM_NAVIGATION, NAVIGATION_RAIL, PERMANENT_NAVIGATION_DRAWER
}

enum class ReplyContentType { //레이아웃 종류.
    LIST_ONLY, LIST_AND_DETAIL
}